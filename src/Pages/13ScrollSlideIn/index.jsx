import styles from './styles.module.scss';

export const ScrollSlideInPage = () => {
  return (
    <section className={styles.page}>
      <div className={styles.container}>
        <h2 className={styles.title}>13</h2>
      </div>
    </section>
  );
};
