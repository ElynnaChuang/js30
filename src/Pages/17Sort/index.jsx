import styles from './styles.module.scss';

const SortPage = () => {
  return (
    <section className={styles.page}>
      <div className={styles.container}>
        <h2 className={styles.title}>17 Page</h2>
      </div>
    </section>
  );
};

export default SortPage;
